# Normalize line endings so Windows and Linux checkouts agree.
# Without this, a Windows session rewrites every tracked file to CRLF and the
# whole tree shows as modified with no real content change.
* text=auto eol=lf

*.png binary
*.ps1 text eol=crlf
